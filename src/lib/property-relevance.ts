/**
 * @deprecated — Fase 20.W3.3: codigo movido pra `@/services/property/relevance`.
 * Este arquivo eh um re-export pra manter compat com imports antigos.
 * Migrar callers gradualmente, depois remover este arquivo.
 */
export { scoreProperty, sortByRelevance } from "@/services/property/relevance"
