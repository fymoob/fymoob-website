/**
 * Layout do Studio — bypassa qualquer chrome do site (header/footer/sidebar).
 *
 * Studio precisa de tela cheia sem interferência de outros estilos.
 */
export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children
}
