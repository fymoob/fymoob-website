# Hipótese H-20260417-004 — experimental.inlineCss

## Believe That
Habilitar `experimental.inlineCss: true` em [next.config.ts](../../../next.config.ts). Next 16.1 + Turbopack substitui `<link rel="stylesheet">` por `<style>` inline no HTML, eliminando request render-blocking de CSS.

## Will Result In

- **LCP mediana cai 100-300ms** — CSS chega junto com HTML, eliminando round-trip de download + parse sequencial
- **FCP mediana cai 150-250ms** — first paint não bloqueado por CSS pendente
- **TBT neutro ou marginal** — não muda JS execution
- **CLS possivelmente melhora** — CSS chega imediatamente, evita paint sem styles que depois shifta
- **HTML cresce ~25KB** — CSS principal `f75161fcad82fcb2.css` (25KB) será inline

**Mecanismo:** Lighthouse v5 audit `render-blocking-resources` apontou `f75161fcad82fcb2.css` (25 KB) como bloqueando FCP ~200ms e LCP potencialmente. Com `inlineCss: true`, esse CSS fica embutido no HTML servido pelo Vercel Edge → zero request adicional, zero round-trip, CSS disponível no parse do HTML.

**Trade-off:** HTML maior (não cacheável entre page loads). Mas gain em First Paint > cost em bytes de network (CSS é entregue uma vez vs múltiplas requests sem cache hit).

## Success Criterion

Em `/busca` mobile, 5 runs pós-deploy:
- **LCP mediana: < 4.70s** (baseline atual pós-H003: 4.89s; meta -200ms = -4%+)
- **FCP mediana: < 1.10s** (baseline 1.23s; meta -100ms)
- **CoV < 10%** (protocolo padrão)
- **TBT mediana: ≤ 600ms** (não regredir vs baseline pós-H003 de 577ms)
- Build passa sem warnings

## Kill Criterion

Reverter `inlineCss: true` → `false` se:
- LCP piorar > 100ms (mediana pós > 4.99s)
- FCP piorar > 80ms
- HTML size crescer > 40KB (overhead demais)
- TBT regredir > 50ms
- Qualquer erro de CSS ausente (componentes renderizando sem estilo)

## Baseline Measurement

**Baseline atual = pós-H003 (reactCompiler ativado)**, não o baseline original.

- **Arquivo:** `docs/perf/baselines/2026-04-17-busca-post-H003.json` (5 runs, CoV 10.3% borderline)
- **Key numbers:**
  - Score: 65
  - LCP: 4894ms (4.89s)
  - TBT: 577ms
  - CLS: 0 mediana (1/5 samples 0.078)
  - FCP: 1385ms
  - Bundle /busca: 782.9 KB
  - HTML /busca: 682 KB (do curl download anterior)

## Predicted Delta

Com base em [Next.js inlineCss docs](https://nextjs.org/docs/app/api-reference/config/next-config-js/inlineCss) e [focusreactive critical CSS article](https://focusreactive.com/critical-css-with-nextjs/):

- **LCP:** -100 a -300ms (faixa) — esperado -200ms ± 80ms
- **FCP:** -100 a -250ms (faixa) — esperado -180ms ± 50ms
- **TBT:** neutro ± 20ms
- **HTML size:** +20-30KB
- **CSS requests bloqueantes:** de 2 atuais para 0

**SUCCESS se LCP cair em qualquer valor dentro da faixa.** Ganhos <50ms classificados como INCONCLUSIVE.

## Attribution Mechanism

### Pré-deploy (local)
- [ ] `curl localhost:3000/busca` após `npm run build && npm start` — verificar se aparece `<style>` inline no HTML em vez de `<link rel="stylesheet">`
- [ ] Pular — difícil testar localmente sem Vercel Edge. Validar direto em prod após deploy.

### Pós-deploy (prod)
- [ ] `curl -sL https://fymoob-website.vercel.app/busca | grep -c "<style"` deve ser > 0 (CSS inline presente)
- [ ] `curl -sL https://fymoob-website.vercel.app/busca | grep -c '<link rel="stylesheet"'` deve ser **menor** que antes (ou 0)
- [ ] `curl -sL https://fymoob-website.vercel.app/busca | wc -c` — HTML size (esperado +20-30KB vs atual 682KB)
- [ ] Se inline CSS não estiver presente, a flag não está funcionando — investigar antes de medir

## Status

- [x] Baseline medido (post-H003 baseline existente)
- [x] Hipótese aprovada (este arquivo committed antes do código)
- [ ] Mudança implementada
- [ ] Build passa
- [ ] Deploy
- [ ] Attribution verificada em prod
- [ ] Post-measurement (5 runs)
- [ ] Delta calculado
- [ ] Success/Kill avaliado

## Delta Medido

*(aguardando pós-deploy)*

## Conclusão

*(aguardando)*
