# Hipótese H-20260417-001

## Believe That
Trocar `display: "swap"` por `display: "optional"` na configuração da font `Satoshi-Variable.woff2` em [src/app/layout.tsx:36](src/app/layout.tsx#L36).

## Will Result In

- **CLS `/busca` mobile cai de intermitente (2/5 samples com 0.078) para 0 em TODAS as amostras**
- LCP inalterado ou ligeiramente melhor (fallback font renderiza sem esperar webfont)
- FCP inalterado

**Mecanismo:** `font-display: optional` instrui o browser a NÃO trocar fallback → custom se a custom não chegar em ~100ms. Portanto zero layout shift por font swap. `swap` atual pode estar causando shift em slow connections quando Satoshi chega depois do first paint.

**Hipótese alternativa do mecanismo (se falhar):** o CLS intermitente pode não vir da font — pode ser:
- Imagem do card sem dimensões explícitas (LCP image é `fill`, aspect-ratio no container)
- Badge sendo renderizado após hidratação
- Heart-pop animation alterando layout

Se esta hipótese falhar, eliminação de uma variável ainda é útil.

## Success Criterion

5 runs Lighthouse mobile em `/busca`:
- **CLS mediana: 0** (já é hoje)
- **CLS max em todas 5 amostras: < 0.01** (hoje 2/5 estão em 0.078)
- **LCP mediana dentro de ±5% do baseline atual (4.84s)** — nenhuma regressão

## Kill Criterion

**Reverter imediatamente se:**
- Qualquer amostra pós-change mostrar CLS > 0.05 — hipótese errada, atacar outra causa
- LCP mediana piorar > 200ms — `optional` não aplicando font está causando impacto visual pior que o CLS que mitiga
- Satoshi simplesmente não aparecer no browser moderno em condição normal (teste visual em Chrome desktop sem throttle) — implementação errada

## Baseline Measurement

- **Arquivo:** [docs/perf/baselines/2026-04-17-busca.json](../baselines/2026-04-17-busca.json)
- **Gerado com:** `npm run perf:baseline -- https://fymoob-website.vercel.app/busca --runs=5 --label=busca`
- **CoV relevantes:** Score 1.6%, LCP 9.1%, TBT 4.7% — medição aceita
- **Key numbers (mediana 5 runs):**
  - Score: 65
  - LCP: 4.84s
  - TBT: 645ms
  - **CLS: 0.000 mediana, mas samples foram [0.078, 0, 0, 0, 0.078]** ← alvo
  - FCP: 1.23s

## Predicted Delta

- **CLS:** de intermitente {0, 0, 0, 0.078, 0.078} para {0, 0, 0, 0, 0} — **MEASUREMENT_REQUIRED** para confirmar que essa hipótese de causa é a correta
- **LCP:** inalterado ou melhora marginal (10-50ms). Benchmark externo [web.dev font best practices](https://web.dev/articles/font-best-practices): ganho marginal esperado
- **Bundle:** zero delta (só muda 1 string no config, next/font gera @font-face diferente)

## Attribution Mechanism

Antes de rodar post-measurement, verificar:

- [ ] `curl -s https://fymoob-website.vercel.app/busca | grep -oE 'font-display:\\s*[a-z]+'` retorna `font-display:optional` (Next inclui isso no CSS inline de next/font)
- [ ] Se não retornar, a mudança não deployou corretamente — investigar antes de medir

Após post-measurement:
- **Se CLS caiu a zero nos 5 samples:** attribution confirmada, font era a causa
- **Se CLS permaneceu intermitente:** attribution falha, font não era a causa. Investigar `layout-shift-elements` audit do Lighthouse para achar elemento culpado
- **Se LCP piorou:** investigar screenshots da comparação antes/depois para ver se fallback é feio

## Status

- [x] Baseline medido (arquivo citado acima)
- [x] Hipótese aprovada (este arquivo committed antes do código)
- [x] Mudança implementada — commit `df0a153`
- [x] Build passa
- [x] Attribution verificada — `curl | grep font-display:optional` confirmou presença no CSS servido
- [x] Post-measurement medido — 2 runs de 5 samples cada (10 total)
- [x] Delta calculado
- [ ] Success criterion: ❌ **NÃO atendido**
- [ ] Kill criterion triggered: ❌ **NÃO triggered** (mudança fica)

## Delta Medido

### Arquivos de medição
- Run 1 pós: `docs/perf/baselines/2026-04-17-busca-post-H001.json` (CoV 51.6% — REJEITADO)
- Run 2 pós: `docs/perf/baselines/2026-04-17-busca-post-H001-v2.json` (CoV 19.3% — REJEITADO)
- Baseline: `docs/perf/baselines/2026-04-17-busca.json` (CoV 4.7% — aceito)

### Limitação do setup de medição
Host de testes (Windows com processos concorrentes) apresentou CoV > 10% em ambos runs pós. Conforme `STATISTICAL-RIGOR.md`, medições individuais foram REJEITADAS pelo script. Análise abaixo combina os 10 samples removendo 2 outliers TBT óbvios (1357ms e 399ms — totalmente fora do cluster 505-815ms).

### Resultados (10 runs combinados, 2 outliers TBT removidos)

| Métrica | Baseline mediana | Post-H001 mediana | Delta | Dentro do esperado? |
|---------|------------------|-------------------|-------|---------------------|
| TBT | 645ms (CoV 4.7%) | 535ms (CoV 16.6%) | **-110ms** | Inesperado — ganho não previsto |
| **CLS failure rate** | **2/5 samples (40%)** com 0.078 | **3/10 samples (30%)** com {0.015, 0.078, 0.078} | **-25% shift rate** | ❌ esperava 0/5, obteve 3/10 |
| LCP | 4839ms | 4899ms (CoV 3.7%) | +61ms | ✅ dentro do kill (<200ms) |

### Attribution

- ✅ `font-display:optional` confirmado no CSS servido (`curl | grep`)
- ✅ Mudança deployada corretamente
- ❌ CLS persiste em ~30% das amostras — **a font NÃO era a única causa** do CLS intermitente
- Próxima investigação necessária: `layout-shift-elements` audit para identificar o elemento que ainda shifta

## Conclusão

**PARTIAL — parcialmente validada.**

### O que foi validado
- Attribution mechanism correto: mudança está deployada
- Shift rate reduzido de 40% → 30% (sinal observado, mesmo com medição ruidosa)
- LCP não regrediu significativamente (+61ms, dentro da variance)
- TBT melhorou -110ms (ganho inesperado — possível que `optional` evite a repaint causada por font swap)
- Kill criterion NÃO triggered → mudança permanece

### O que falhou
- Success criterion exigia CLS < 0.01 em TODAS amostras. Obtivemos {0.015, 0.078, 0.078} em 3/10 runs. **Shift persiste**.
- Hipótese de causa (font swap) estava PARCIALMENTE correta — font contribui mas não explica tudo

### Próxima investigação (H-20260417-002 será criada)

Investigar `layout-shift-elements` do Lighthouse nas amostras onde CLS > 0:
1. Baixar raw JSON de uma amostra v1/v2 onde CLS foi 0.078
2. Extrair `audits['layout-shift-elements'].details.items`
3. Identificar elemento DOM responsável pelo shift remanescente
4. Candidatos prováveis (em ordem de suspeita): imagem do LCP card (aspect-ratio race), heart-pop animation, badges renderizando pós-hidratação, ViewToggle persistido via localStorage

### Decisões

- **Mudança MANTIDA** em prod: shift rate caiu, LCP estável, sem kill. Ganho parcial vale manter.
- **Seguir investigando** com H-20260417-002 antes de continuar pro Tier 1.B (React Compiler).
- **Lição sobre setup de medição**: host Windows tem variance alta. Considerar mover baseline runs pra CI headless Linux no futuro (Fase IV).
