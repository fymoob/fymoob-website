# Hipótese H-20260417-007 — ISR com cacheTag + revalidateTag

## Believe That
Adicionar `tags: ["imoveis"]` ao `unstable_cache` de `getCachedProperties` em [src/services/loft.ts:398](src/services/loft.ts#L398) e criar endpoint `POST /api/revalidate` que aceita `{ tag }` protegido por `REVALIDATE_SECRET` para invalidação granular.

## Will Result In

### Ganhos esperados
- **Invalidação sob demanda** — quando inventário Loft muda (imóvel vendido/novo), `curl -X POST /api/revalidate` com body `{"tag":"imoveis"}` purga o cache imediatamente, sem esperar o TTL de 1h
- **Zero impacto em performance lab** — cache já existia, só ganha mecanismo de invalidação. Lighthouse: neutro
- **TTFB mantido ou melhor** — mesma arquitetura de cache, só tag adicionada
- **Preparação pra futuro** — base pra `cacheComponents` + `"use cache"` directive do Next 16.3+

### O que NÃO é objetivo
- Esta hipótese NÃO remove o `staleTimes: 30` atual (issue separada, decisão pós field data)
- NÃO é refactor grande — é adição opcional à arquitetura atual

## Success Criterion

- **Build passa** sem erros
- **Endpoint `/api/revalidate` retorna 200** com secret correto + body válido
- **Endpoint retorna 401** sem secret ou com secret errado (auth protegido)
- **Endpoint retorna 400** com body inválido (validation)
- **Invalidação funciona**: após POST com `{"tag":"imoveis"}`, próxima chamada a `getProperties()` re-fetch à Loft API (confirmar via logs)
- **Cache hit normal antes da invalidação** (não quebra o cache existente)
- **Zero regressão funcional** — `/busca`, `/imovel/*`, `/imoveis/[bairro]/*` continuam renderizando igual

## Kill Criterion

Reverter se:
- Build quebrar
- Cache parar de funcionar (TTFB de `/busca` subir > 500ms)
- Endpoint retornar 500 consistentemente
- `revalidateTag` não invalidar (teste manual falha)
- Algum comportamento inesperado em produção (erro de runtime)

## Baseline Measurement

- **Arquivo baseline:** `docs/perf/baselines/2026-04-17-busca.json` (pós-H003)
- **Estado atual:** Score 65, TBT 577ms, LCP 4.89s
- **Bundle /busca:** 781 KB (pós-revert H-005)
- **Cache atual:** `unstable_cache` com `revalidate: 3600` (1h) sem tags

## Predicted Delta

| Métrica | Delta esperado |
|---------|----------------|
| Score Lighthouse | **0** (neutro) |
| LCP | **0** (neutro) |
| TBT | **0** (neutro) |
| Bundle | **0** (mudança é server-side) |
| HTML size | **0** (sem impacto em render) |
| TTFB cold | Marginalmente melhor (mesmo cache, melhor observabilidade) |
| **Novo**: invalidação imediata | **de 1h → <1s** quando chamado |

Esta hipótese é primariamente **operacional**, não perf. Ganho é controle + invalidação on-demand. Perf segue igual.

## Attribution Mechanism

### Pré-deploy
- [ ] Ler current `getCachedProperties` em `src/services/loft.ts:398`
- [ ] Confirmar `REVALIDATE_SECRET` não existe ainda em nenhum lugar
- [ ] Verificar pattern de INDEXNOW_SECRET como referência para auth

### Pós-deploy
- [ ] `curl -X POST https://fymoob-website.vercel.app/api/revalidate` (sem secret) → **401**
- [ ] `curl -X POST ... -H "x-revalidate-secret: WRONG"` → **401**
- [ ] `curl -X POST ... -H "x-revalidate-secret: $SECRET" -d '{}'` → **400** (sem tag)
- [ ] `curl -X POST ... -H "x-revalidate-secret: $SECRET" -H "Content-Type: application/json" -d '{"tag":"imoveis"}'` → **200**
- [ ] Antes da invalidação: `/busca` carrega rápido (cache hit server-side)
- [ ] Após invalidação: próxima request re-fetch Loft (verificar logs Vercel)

### Validação funcional
- [ ] `/busca` continua listando 24 imóveis na página 1
- [ ] `/imovel/<slug>` continua funcionando
- [ ] Filtros em `/busca` continuam filtrando

## Arquivos afetados

| Arquivo | Mudança | Linhas |
|---------|---------|--------|
| [src/services/loft.ts](src/services/loft.ts) | `tags: ["imoveis"]` em `getCachedProperties` | +1 |
| `src/app/api/revalidate/route.ts` (NOVO) | POST handler com auth + revalidateTag | +30 |
| `.env.local` / Vercel | `REVALIDATE_SECRET` env var | +1 env |

**Total: ~31 linhas, 2 arquivos modificados (1 criado).**

## Status

- [x] Baseline
- [x] Hipótese aprovada
- [ ] Implementação
- [ ] Build passa
- [ ] Deploy
- [ ] Attribution tests em prod (4 curl scenarios)
- [ ] Conclusão

## Delta Medido

*(aguardando)*

## Conclusão

*(aguardando)*
