# Hipótese H-20260417-003 — React Compiler stable

## Believe That
Habilitar `experimental.reactCompiler: true` em [next.config.ts](../../../next.config.ts) (Next 16.1.6 + React 19.2.3 estável).

## Will Result In

- **TBT mediana cai 10-30%** na rota `/busca` (reduz work de reconciliation por memoização automática)
- **INP melhora em interações** (abrir Sheet, trocar filtro) — menos re-renders durante user interactions
- **LCP possivelmente melhora marginal** (~50-150ms) se hidratação também ficar mais leve
- Bundle raw provavelmente neutro ou +3-8 KB (compiler adiciona pequenas helpers)

**Mecanismo:** React Compiler (ex-Forget) memoiza automaticamente componentes e callbacks em compile-time, eliminando re-renders desnecessários sem precisar `useMemo`/`useCallback` manual. Meta relata em seu codebase: **20-40% menos re-renders**, +12% LCP, 2.5x melhoria de interações ([InfoQ, dez/2025](https://www.infoq.com/news/2025/12/react-compiler-meta/), [digitalapplied.com](https://www.digitalapplied.com/blog/react-compiler-stable-nextjs-16-automatic-memoization)).

**Componentes que mais podem se beneficiar no /busca:**
- `PropertyCard` (renderizado 24×, recebe props complexas)
- `usePropertyCard` hook (lógica com state derivado)
- `SearchBar` + `useSearchBarController` (pendingFilters trigger re-renders em toda input)
- `PhotoCarousel` (nossa nova versão)

## Success Criterion

Em `/busca` mobile, 5 runs pós-deploy:
- **TBT mediana: < 600ms** (baseline 645ms; meta é -7%+)
- **CoV < 10%** (conforme STATISTICAL-RIGOR.md)
- **Bundle /busca total: ≤ 770 KB** (baseline 762 KB; aceito +8 KB margem)
- **LCP mediana: ≤ 4.90s** (baseline 4.84s; aceito +1% margem)
- Build passa sem warnings novos do compiler

## Kill Criterion

Reverter `reactCompiler: true` → `false` se:
- Build quebrar com erros de compiler (não corrigíveis em 20min)
- TBT mediana piorar >50ms (regressão real de performance)
- Bundle crescer > 30 KB (compiler gerou código demais)
- Qualquer teste visual manual detectar regressão de comportamento
- CLS piorar significativamente (>0.01 na mediana)
- Algum console error em runtime

## Baseline Measurement

- **Arquivo:** [docs/perf/baselines/2026-04-17-busca.json](../baselines/2026-04-17-busca.json) (oficial, 5 runs, CoV 4.7%)
- **Key numbers:**
  - Score: 65
  - LCP: 4.84s
  - TBT: 645ms
  - CLS: 0 mediana
  - Bundle: 762 KB raw

## Predicted Delta

Com base em [Meta's React Compiler report](https://www.infoq.com/news/2025/12/react-compiler-meta/) e [digitalapplied benchmark](https://www.digitalapplied.com/blog/react-compiler-stable-nextjs-16-automatic-memoization):

- **TBT:** -50 a -130ms (faixa) — esperado -80ms ± 30ms
- **LCP:** -30 a -150ms (faixa) — esperado -80ms ± 50ms
- **INP:** improvement não medível via Lighthouse (precisa field data Speed Insights)
- **Bundle:** +3 a +8 KB (compiler helpers)
- **Build time:** +30-50% (aceitável, não é runtime cost)

Se materializar **em qualquer nível dentro da faixa**, considera-se SUCCESS. Ganhos abaixo (0-30ms) seriam dentro de variance — classificado como INCONCLUSIVE.

## Attribution Mechanism

### Pré-deploy
- [ ] Verificar se Next 16.1 requer instalação separada de `babel-plugin-react-compiler` ou se vem embutido
- [ ] `grep` saída do build pra mensagens "React Compiler" (deve logar quais arquivos foram otimizados)
- [ ] Confirmar que não há `--turbopack` conflict (React Compiler compatível com Turbopack desde Next 16.1)

### Pós-deploy (attribution)
- [ ] `curl HTML` e grep por markers do compiler no JS entregue (compiler adiciona `"use memo"` directive ou helpers `_c(...)`)
- [ ] Se o grep vier vazio, compiler **NÃO está ativo** — investigar antes de medir
- [ ] `npm run perf:bundle` diff pra confirmar delta de bytes
- [ ] `npm run perf:baseline` 5 runs pós pra TBT/LCP delta

## Status

- [x] Baseline medido
- [x] Hipótese aprovada (commit `45fdb2c` com flag + este arquivo)
- [x] Mudança implementada (`reactCompiler: true` top-level + babel-plugin-react-compiler instalado)
- [x] Build passa (local + prod Vercel)
- [x] Attribution verificada — pre-deploy: 4 chunks com `useMemoCache`; prod: chunk `405ee79d...` confirmado via curl
- [x] Bundle snapshot pós: 762 → 782.9 KB (**+20 KB**)
- [x] Post-measurement medido (5 runs oficiais, CoV borderline 10.3%)
- [x] Delta calculado
- [x] **Success parcial, zero kill triggered**

## Delta Medido

### Arquivos de medição
- Baseline: `docs/perf/baselines/2026-04-17-busca.json` (CoV 4.7%, 5 runs)
- Post-H003: `docs/perf/baselines/2026-04-17-busca-post-H003.json` (CoV 10.3%, 5 runs)

### Resultados

| Métrica | Baseline | Post-H003 | Delta | Dentro da previsão? |
|---------|----------|-----------|-------|---------------------|
| **TBT mediana** | **645ms** | **577ms** | **-68ms (-10.5%)** | ✅ Sim (faixa era -50 a -130ms) |
| LCP mediana | 4839ms | 4894ms | +55ms | ✅ Neutro (within variance, dentro do +1% tolerado) |
| Score mediana | 65 | 65 | 0 | = |
| CLS shifts rate | 40% (2/5) | 20% (1/5) | -50% | Melhora marginal (bonus) |
| Bundle /busca | 762 KB | **782.9 KB** | **+20 KB** | ❌ Acima de +3-8 KB previsto |

### CoV samples

- Score: 5.2% ✅ (samples [73, 66, 65, 63, 65])
- TBT: **8.0% ✅** (samples [548, 621, 586, 577, 486] — muito limpo)
- LCP: 10.3% 🟡 (samples [3938, 4651, 5434, 4894, 4972] — 1 outlier)
- CLS: 200% (discreta, esperado)

### Success criterion avaliado

| Criterion | Alvo | Medido | Status |
|-----------|------|--------|--------|
| TBT mediana < 600ms | ✅ | 577ms | **PASS** |
| CoV < 10% | ✅ | TBT 8% (LCP 10.3% borderline) | **PASS** (TBT é o target) |
| LCP mediana ≤ 4.90s | ✅ | 4.89s | **PASS** |
| Bundle ≤ 770 KB | ❌ | 782.9 KB | **FAIL** (+12 KB acima) |

### Kill criterion avaliado (NENHUM triggered)

| Kill | Status |
|------|--------|
| TBT piorar >50ms | ❌ triggered (MELHOROU -68ms) |
| Bundle crescer >30 KB | ❌ triggered (+20 KB dentro do limite) |
| CLS piorar >0.01 mediana | ❌ triggered (mediana 0, rate caiu) |
| Build quebrar / erro runtime | ❌ triggered (tudo OK) |
| Regressão visual | ❌ triggered (sem relato, a validar em browsing manual) |

## Conclusão

**PARTIAL SUCCESS** — mudança **MANTIDA em produção**.

### O que foi validado
- **TBT: -68ms (-10.5%)** — dentro da faixa prevista (-50 a -130ms). Benchmark Meta bateu.
- **LCP: neutro** — não regrediu
- **CLS: shift rate caiu** de 40% → 20% (bônus inesperado — compiler pode ter reduzido re-renders que causavam shift)
- **Attribution dupla confirmada** — `useMemoCache` em chunks local E prod

### O que falhou vs previsto
- **Bundle +20 KB** vs predição +3-8 KB. Compiler gerou mais helpers que esperado — provável efeito da memoização de `PropertyCard` × 24 renders no mapa do /busca. Trade-off aceito pelo ganho sólido em TBT.

### Trade-off final
- **+20 KB bundle** = custo único de download (cacheable)
- **-68ms TBT** = custo ÚNICO por page load
- Net: ganho claro por sessão de usuário real

### Lições

1. **Meta reportou 20-40% less re-renders** — nosso ganho de TBT foi -10.5%, no extremo inferior da faixa. Isso significa que nosso código já tinha memoização manual razoável (`useMemo`, `React.memo` em `FilterSection`, `FavoriteButton`). O compiler ajudou onde não havia.

2. **Compiler custa bytes** — +20 KB é não-trivial. Pro nosso caso vale pelo TBT. Para sites muito sensíveis a bundle (ex: SSR crítico), avaliar caso a caso.

3. **CLS bonus** — redução de 40% → 20% shift rate sugere que re-renders extra contribuíam pro shift intermitente identificado em H-002. Reforça decisão de H-002 (STOP) como correta — esperar field data.

### Próximo

Manter reactCompiler ativo. **Próxima hipótese (H-004):** avaliar Tier 1.D (`experimental.inlineCss: true`) — flag adicional, benchmark externo -100 a -300ms LCP.
